# Flask POC

Minimal Flask examples, mirroring `fastapi_poc/` for comparison.

```sh
pip install flask
flask --app flask_poc.01_helloworld run
curl http://127.0.0.1:5000
```

| File | Demonstrates |
|---|---|
| `01_helloworld.py` | Minimal app and route |
